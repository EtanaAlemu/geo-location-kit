# Copy into local.properties (gitignored; keep sdk.dir=...)

# Test Maven Central release in :app (no token)
geo.usePublished=true
geo.publishedGroup=io.github.etanaalemu
geo.version=1.0.2

# Local modules (default for development)
# geo.usePublished=false

# GitHub Packages (optional; classic PAT with read:packages)
# gpr.user=YOUR_GITHUB_USERNAME
# gpr.key=ghp_...
